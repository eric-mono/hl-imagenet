# Phase 2 Verify Mining Archive

This folder preserves one-shot scripts used to mine or generate Phase 2 verify
rules. They are not part of the current public evaluation path.

The current classifier lives in `hlinet/classifier/predict.py`; current
reporting should use the `base_rerank` and `full` modes documented in the root
README and `docs/phase2/blog.md`.
